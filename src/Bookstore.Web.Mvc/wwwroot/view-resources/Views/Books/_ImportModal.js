(function ($) { 
    var importService = abp.services.app.bookImport,
        l = abp.localization.getSource('Bookstore'),
        _$modal = $('#BookImportModal'),
        _$form = _$modal.find('form');
})(jQuery);